output   = "my_models"
wipe     = true
no-tests = true
add-enum-types = true

[psql]
  dbname = "scc-pg"
  host   = "localhost"
  port   = 5432
  user   = "postgres"
  pass   = "postgres"
  sslmode = "disable"
  blacklist = ["schema_migrations"]